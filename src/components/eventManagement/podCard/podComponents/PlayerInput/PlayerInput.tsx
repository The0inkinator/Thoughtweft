import { Event } from "~/typing/eventTypes";
import styles from "./playerInput.module.css";

interface PlayerInputInputs {
  eventData: Event;
  podId: number;
}

export default function PlayerInput({ eventData, podId }: PlayerInputInputs) {
  return <></>;
}
